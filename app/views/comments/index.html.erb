
<h2>Comments</h2>
<% @user.comments.each do |comment| %>
  <p>
    <strong>Commenter:</strong>
    <%= comment.commenter %>
  </p>

  <p>
    <strong>Comment:</strong>
    <%= comment.aunty %>
  </p>
  
<%= link_to 'edit', edit_user_comment_path(japan.id) %>

<% end %>
